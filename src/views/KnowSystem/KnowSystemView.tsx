import * as React from "react";
import { StyleSheet, Text, View, TouchableOpacity, Image } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#F5FCFF"
  },
  welcome: {
    fontSize: 30,
    textAlign: "center",
    margin: 10
  }
});

export default class KnowSystemView extends React.Component<any, any> {
  render() {
    return <Text>2424</Text>;
  }
}
